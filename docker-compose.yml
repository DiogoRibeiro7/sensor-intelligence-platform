services:
  sensor-api:
    build: .
    command: uvicorn sensor_intelligence.api.app:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./artifacts:/app/artifacts
